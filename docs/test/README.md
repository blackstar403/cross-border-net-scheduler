# 测试

**本模块回答：** 需求是否被对照过；限制写清楚。

**钱潮杯完成：** 用例覆盖全部 `REQ-*`；三场景与 `docs/mvp.md` 演示脚本一致；有带环境与结果的报告。

| 计划文档 | 内容 |
|---|---|
| `plan.md` | 用例列表，引用 REQ 编号 |
| `report.md` | 实测：环境、步骤、期望、实际、缺陷 |

自动测试放仓库根 `tests/`。没有「隔离开/关」「争抢开/关」对照，不算测过调度。

状态：索引已建。应先于或并行于代码编写 `plan.md`。

# 部署上线

**本模块回答：** 演示环境如何在干净机器上复现。

**钱潮杯的「上线」= 单机可启动 + 运行手册。** 不是公网 SaaS、不是多办公室交付。

| 计划文档 | 内容 |
|---|---|
| `local.md` | 依赖、`.env.example`、启动命令 |
| `runbook.md` | 演示三步、代理失败、无权限限速时的处理 |

脚本与编排放仓库根 `deploy/`。

状态：索引已建。有可运行二进制/脚本后再写 local.md。

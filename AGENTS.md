# AGENTS.md

本仓库是钱潮杯项目，不是通用跨境专线，也不是德邻杯创业 BP。

## 产品边界（改代码前先对这一段）

只做控制面，不拥有底盘：

1. 产品是电商出海网络的运维与智能调度。接入卖家已有宽带、Clash/代理、SD-WAN 或备用网络，不卖节点和线路，不自建 POP、不承诺新带宽。
2. 调度围绕后台操作、同步、直播等经营任务。单线路、单订阅也可做观测和诊断；多线路、双出口不是产品成立或首版验收的前提。
3. 物理上限写进文档、不要写进承诺：中美延迟、国际出口晚高峰、最后一公里、平台风控，本项目破不了。

## 仓库结构

完整项目七段的完成定义见 `docs/PROJECT.md`。改任何一段前先对完成线和追溯规则：需求有编号，设计/测试/演示引用同一编号；调研标为做不到的不得写成目标。

钱潮杯完成 = 左列（可演示闭环）。产品级多租户/持证线路/融资 BP 不在本阶段。

## MVP 围绕一个运维闭环

见 `docs/mvp.md`。首版从调研中选择一个高频、可处理的经营问题，完成「接入已有资源 → 发现业务异常 → 判断原因 → 执行受控动作或给出建议 → 验证恢复并记录」。新增功能必须服务该闭环，有证据、编号需求和验收条件；不能因定位变宽就建设通用网络管理平台。

1. 业务可见是闭环入口：说明哪个作业受影响、证据是什么；不能只做 Mbps 仪表盘，不能把所有业务失败归因于网络。
2. 动作按现有资源、平台能力和业务约束执行。没有可用动作时明确建议或升级给服务商，不伪造自动恢复；声明执行能力须有实际验证。
3. 店铺出口绑定是可选场景及业务约束，直播让路是候选调度案例，不是两项必做功能。需要出口稳定的业务不得为测速或故障回退而静默换出口。
4. 跨平台是产品方向，依据使用方设备确定验证优先级。共用控制面与平台执行适配分开，未经测试的平台不宣称支持。

## 叙事禁区

不要写、不要实现、不要在比赛材料里暗示：

- 全球加速、自建海缆、替代 Cloudflare / 阿里云 GA
- 保证不封号、保证不关联
- 全面翻墙、通用 VPN
- 把 Shopify 买家访问再套一层代理当「加速」

## 比赛约束

- 当前只打钱潮杯（跨境经营类）。德邻杯 BP 不做。
- **目标 1（杯赛）优先于目标 2（产品演进）。** 报名及材料均在 2026-09-16 北京时间 24:00 截止，提交后不可改，材料须先过报名审核；用户确认 **2026-09-11 核对后报名，先向官方确认审核时效**（`docs/competition/official.md`、`docs/competition/registration.md`）。不为多租户/线路采购插队。见 `docs/GOALS.md`。
- 材料要能给普通创业者评委讲懂：作业成功率，不是 BGP。
- 仓库保持 private，除非用户明确要求改 public。
- **git commit 说明用中文**（标题一行说清做了什么，必要时正文补原因）。不要用英文 commit message。

## 实现偏好

- 先可演示，再谈架构。Demo 对一个有编号的作业异常走通发现、判断、处理和复测，包含处理失败或无可执行动作的分支。
- 自然语言运维可采用通用 LLM、领域流程/知识库、工具调用和记忆；执行权限与业务约束由工具层落实，不用聊天演示冒充实际任务完成。
- 密钥、账号、真实店铺 ID 不进仓库。

# AGENTS.md

本仓库是钱潮杯项目，不是通用跨境专线，也不是德邻杯创业 BP。

## 产品边界（改代码前先对这一段）

只做控制面，不拥有底盘：

1. 假设卖家已有家宽 / 低价 SD-WAN / 4G 备份。本项目调度这些路径，不自建 POP、不承诺新带宽。
2. 调度对象是店铺、场次、应用类型，不是「再加 5Mbps」。
3. 物理上限写进文档、不要写进承诺：中美延迟、国际出口晚高峰、最后一公里、平台风控，本项目破不了。

## 仓库结构

完整项目七段的完成定义见 `docs/PROJECT.md`。改任何一段前先对完成线和追溯规则：需求有编号，设计/测试/演示引用同一编号；调研标为做不到的不得写成目标。

钱潮杯完成 = 左列（可演示闭环）。产品级多租户/持证线路/融资 BP 不在本阶段。

## MVP 只允许这三件事

见 `docs/mvp.md`。新增功能前问：它是不是这三项之一的最小实现？不是就先记到文档，不要开工。

1. 店铺隔离：同办公室两店，出口 IP 不同，能证明未串线。
2. 直播优先：开播时压低其他上行，能看见码率 / 丢包 / 是否可能锁 TCP。
3. 业务可见：看板是后台打开时间、同步失败次数、本场直播是否降质，不是 Mbps 仪表盘。

## 叙事禁区

不要写、不要实现、不要在比赛材料里暗示：

- 全球加速、自建海缆、替代 Cloudflare / 阿里云 GA
- 保证不封号、保证不关联
- 全面翻墙、通用 VPN
- 把 Shopify 买家访问再套一层代理当「加速」

## 比赛约束

- 当前只打钱潮杯（跨境经营类）。德邻杯 BP 不做。
- 材料要能给普通创业者评委讲懂：作业成功率，不是 BGP。
- 仓库保持 private，除非用户明确要求改 public。

## 实现偏好

- 先可演示，再谈架构。Demo 能跑过「两店不串线 + 开播让路 + 三个业务数字」。
- 策略可以是规则 + 小模型 / 分类，不要为了「AI」硬接聊天大模型。
- 密钥、账号、真实店铺 ID 不进仓库。
